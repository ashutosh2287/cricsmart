# Commentary model artifacts

This directory stores commentary ranker artifacts, metadata manifests, and retrieval-aware model bundles.

Key contracts and lineage files:
- `feature_contract.json` (canonical feature order/types/encodings/schema hash)
- `runtime_thresholds.json` (confidence thresholds for runtime fallback)
- `model_metadata.json` (model lineage + training metadata)
